// Criar uma função que recebe um número como parâmetro e retorna o resultado da multiplicação desse número por ele mesmo.

function multiplicacaoNumeroPorElemesmo(numero) {
    return numero * numero;
}

var resultado = multiplicacaoNumeroPorElemesmo(5);

console.log(`A multiplicação de 5 por ele mesmo é ${resultado}.`);